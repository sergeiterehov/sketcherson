const basic = "--color-icon-basic";
const accent = "--color-icon-accent";

const iconColor = {
  definition: {
    basic,
    accent,
  },
  basic: `var(${basic})`,
  accent: `var(${accent})`,
};

export default iconColor;
